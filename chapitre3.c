#include<stdio.h>

int main() {

        int height, length, width, volume, weight;
        height = 8;
        length = 12;
        width = 10;
        volume = height * length * width;
        weight = (volume + 165) / 166;


        printf("Dimensions: %dx%dx%d\n", length, width, height);
        printf("Volume (cubic inches): %d\n", volume);
        printf("Dimensional weight (pounds): %d\n", weight);
       

        


        return 0;

        /*
        reponce a question => n'est pas affiche aucune message d'avertissement .

        section 1.2

        a/ les directives => #include 
         les instructions =>  int main()
                printf("available for its completion.\n");
        b/ la sortie de programme 
                        Parkinson's Law:
                        Work expands so as to fill the time
                        available for its completion.
        
        section 1.7
        5. les element correct a declaration 
                b c d
        7. for if while 

        8. le nombre de jetons dans l'exemple: answer=(3*q-p*p)/3; 
                est 14;

        9 
        answer = ( 3 * q - p * p ) / 3 ;

        10
        les espaces sont essentiles c'est entre les ligne d'affichage





        
        */
       
}